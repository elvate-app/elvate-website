import { styled } from "@mui/material";
import { ReactComponent as Bar } from "src/assets/svg.svg";

export const BarSvg = styled(Bar)`
  position: absolute;
  z-index: -10;
`;

export default BarSvg;
